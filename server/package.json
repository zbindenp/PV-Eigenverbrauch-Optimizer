{
  "name": "pvoptimizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "example": "node index.js",
    "server": "node server.js",
    "init": "npm i && cd ../pvoptimizer && npm i && cd ../server && npm run deploy-client",
    "deploy-client": "rm -rf client && cd ../pvoptimizer && npm run build-prod && cp -r dist/pvoptimizer ../server/client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/ai-anomaly-detector": "^3.0.0-beta.3",
    "@azure/ms-rest-js": "^2.6.0",
    "csv-parse": "^5.0.3",
    "express": "^4.17.1",
    "lodash": "^4.17.21",
    "node-fetch": "^2.0.0"
  }
}
